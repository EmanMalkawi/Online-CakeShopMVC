@model Cake.Models.Cakeshop

@{
    ViewData["Title"] = "Delete";
}

<div class="a">
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Cakeshop</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Cakeshopname)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Cakeshopname)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Cakeshopid" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-action="Index">Back to List</a>
        </form>
    </div>
</div>